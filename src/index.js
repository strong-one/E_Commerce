import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

// first parameter is the entire app, second argument points to root div (public/index.html)
ReactDOM.render(<App />, document.getElementById("root"));
